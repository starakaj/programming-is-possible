{
  "name": "02-heroku",
  "version": "1.0.0",
  "description": "Lesson 02: Server-side rendering with React, and deploying the whole stupid thing to Heroku",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir dist --source-maps inline",
    "serve": "npm run build && node server/index.js --PORT=3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Tarakajian",
  "license": " CC-BY-SA-4.0",
  "devDependencies": {
    "@babel/cli": "^7.1.0",
    "@babel/core": "^7.1.0",
    "@babel/preset-env": "^7.1.0",
    "@babel/preset-react": "^7.0.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^16.5.2",
    "react-dom": "^16.5.2"
  }
}
